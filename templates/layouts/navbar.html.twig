<header>
	<nav>
		<div class="logo">
			<h1>Green Goodies</h1>
			<img src="{{ asset('images/Logo.svg') }}" alt="Green Goodies">
		</div>
		<ul>
			{% if app.user %}
				<li>
					<a href="{{ path('app_products') }}#products">Nos produits</a>
				</li>
				<li>
					<a href="{{ path('app_cart_show') }}">Mon panier</a>
				</li>
				<li>
					<a href="{{ path('app_profile') }}">Mon compte</a>
				</li>
				<li>
					<a href="{{ path('app_logout') }}">Déconnexion</a>
				</li>
			{% else %}
				<li>
					<a href="{{ path('app_login') }}">Connexion</a>
				</li>
				<li>
					<a href="{{ path('app_register') }}">Inscription</a>
				</li>
			{% endif %}
		</ul>
	</nav>
</header>
